import React from 'react';

const Score = () => {
    return (
        <div className='scoreTop'>
            Score:{state.currentScore} | Top Score: {state.topScore}
        </div>
    );
};

export default Score;
